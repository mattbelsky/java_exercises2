1) What is a Java package?

a group of related classes, interfaces, etc.


2) What is the CLASSPATH variable all about?

specifies the directory path of a package


3) What does the package structure mirror?

directories


4 How do packages affect access to Java classes?

They provide a protected access modifier that only allows other classes or subclasses within the package to access each
other.


5) What is an interface?

code containing empty methods that a class that implements it must use


6) What makes interfaces unique?

Its methods are empty.


7) What keyword is used to specify that a class is going to use a separate interface?

implements


8) What makes interfaces so useful?

They ensure consistency across code because they ensure that certain classes will contain necessary or desired functions.


9) What was added in JDK 8 that makes interfaces even more useful?

default methods


10) How many interfaces can a single class implement?

any number